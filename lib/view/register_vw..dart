import 'package:flutter/material.dart';

class RegisterVw extends StatelessWidget {
  const RegisterVw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register form'),
      ),
    );
  }
}
